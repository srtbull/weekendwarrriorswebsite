/** Plain-text suffix (e.g. metadata). Includes the word Discord — no HTML. */
export const HIGHLIGHTS_LIST_SUFFIX =
  " — full plugin list in Discord.";

/** Fragment before the Discord link (leading space + em dash). */
export const HIGHLIGHTS_SUFFIX_BEFORE_DISCORD = " — full plugin list in ";

/** After the linked word “Discord”. */
export const HIGHLIGHTS_SUFFIX_AFTER_DISCORD = ".";
